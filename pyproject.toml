[project]
name = "class_demo"
version = "0.1.0"
description = "User management and authentication service"
authors = [{ name = "User Service Team" }]
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
